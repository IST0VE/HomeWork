#include <iostream>

int x;

int main()
{
	setlocale(0, "");

	std::cout << "Введите год: ";
	std::cin >> x;

	if (x%4==0 && x%100!=0 || x%400==0)
	{
		std::cout << x << " год является високосным";
	}
	else
	{
		std::cout << x << " год не является високосным";
	}
}
